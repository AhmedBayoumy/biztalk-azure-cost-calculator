# ── AI for free-text parsing ──
# Option 1 (Recommended): GitHub token — free if you have GitHub Copilot
# Create one at https://github.com/settings/tokens (no special scopes needed)
GITHUB_TOKEN=your-github-personal-access-token-here

# Option 2: OpenAI API key (used as fallback if GITHUB_TOKEN is not set)
# OPENAI_API_KEY=your-openai-api-key-here
